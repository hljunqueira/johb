[build]
dockerfilePath = "Dockerfile.api"

[deploy]
startCommand = "uvicorn railway_server:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
